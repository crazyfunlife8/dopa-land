# 多巴樂園．總待辦 v1（單一事實來源）

> 建立：2026-07-28｜這份是**巴巴樂園所有要做的事的總索引**，狀態以此檔為準。
> 一句話：**AI 付費解讀（收入主錨）＋ 大廳社交層（留存 bonus）**，前端先驗意願、要存帳號那刻才上後端。
> 關聯：付費主線 `付費解讀_規劃書_v1.md`｜社交層 `多巴樂園_社交配對層_規劃書_v1.md`｜五寶 `泥巴四寶_人設與口頭禪.md`｜網站 `人類圖網站/`

**狀態圖例**：✅ 完成　🟡 進行中／部分　⬜ 未開始

**開發順序**：核心產品（A）＝主線先打通金流 → 社交 Phase 0 樹洞 → Phase 1 摩天輪 → Phase 2 雲霄飛車；☕ 老師市集為獨立重軌，平行推進不擋主線。

---

## A. 核心產品：巴巴驗證機 ＋ AI 付費解讀（收入主錨）

| # | 任務 | 狀態 | 備註 |
|---|---|---|---|
| A1 | 計算核心（JS 算盤） | ✅ | 已驗證、接進網站；星曆對 JPL <8.2 角秒；交點預設平交點可切、DST 走 MVP 固定 offset |
| A2 | 深度文案（長版 12 段） | ✅ | 已寫＋已 inject＋前端 render 驗收；命理逐段對知識庫稽核並修正（生產者比例／定義標籤塊數／人生角色去「35歲」誤框／無通道措辭＋連帶修 KB04 角度）｜2026-07-28 |
| A3 | 99 元金流串接 | ✅前端／⬜串接 | **前端結帳頁已建（2026-08-03）**：`view-checkout`＝訂單摘要／三種付款方式（信用卡・ATM 虛擬帳號・超商代碼）／**必勾同意框**＋`view-payresult` 三種結果（成功／ATM 取號待繳／超商代碼待繳，含繳費期限、複製鈕）。**必勾框已實作並驗證：沒勾就按付款→擋下、紅框震動、不建立訂單**；勾了才送，且同意時間戳 `consentAt` 存進訂單留證。**待後端**：選金流服務商（綠界／藍新）、導轉、**入帳以服務商伺服器端回拋為準**（不能信前端導回參數）、ATM／超商防盜刷、自動對帳。 |
| A4 | 付款 → 解鎖深度解析 流程 | ✅前端／⬜串接 | **已接（2026-08-03）**：解鎖鈕不再直接解鎖，改走 驗證→結帳→付款成功→解鎖報告。ATM／超商未繳費前不解鎖。示範版有一顆「（示範）我已完成繳費」模擬入帳，正式版要拿掉（入帳由金流回拋驅動）。 |
| A5 | 圖卡下載（9:16 ＋ 1:1 PNG） | ✅ | 解鎖後兩顆按鈕：限動 9:16 直式／貼文 1:1 方形（雙欄佈局）；五型自動套色／巴巴／金句／個人結果，純前端 SVG→canvas 無依賴、匯出無污染實測過｜2026-07-28。（PDF 變體尚未做） |
| A6 | A 層：LLM 自由提問加值 | ⬜ | 未來；須後端藏金鑰＋餵算好的事實、禁自編類型／權威 |

---

## B. 社交配對層：多巴樂園大廳四區

| # | 園區 | 狀態 | Phase | 備註 |
|---|---|---|---|---|
| B1 | 🎠 旋轉木馬＝樹洞紙條牆 | ✅ | **0（最先）** | 大廳入口頁＋樹洞投稿（五型身份／引導模板不問性別／100 字限）＋紙條牆（假資料＋即時上牆「審核中」示範）；純前端空殼實測可運作｜2026-07-28。**2026-08-03 加按讚**：每張紙條右下一顆愛心（點了 +1、再點取消），新投稿的紙條也會帶按讚鈕。**後端串接完成（2026-08-18）**：lounge_posts 真儲存、like_count 由 sync_like_count trigger 維護、按讚去重 RLS（同帳號同張只能一次）、只顯示 status='approved' 貼文。**「我的紙條」＋檢舉完成（2026-08-18）**：「我的紙條」按鈕（登入後顯示）→ 顯示自己所有紙條含 status（審核中／已顯示／未通過＋退件原因）；每張紙條右下 ⚑ 檢舉按鈕 → 寫入 reports 表（reporter_id / reported_id / reason / evidence.body）。**仍待**：人工先審後顯（D2）。 |
| B2 | 🎡 摩天輪＝依結果每日配對 | ✅ | 1 | **前端配對牆已建（2026-07-29，改每日制）**：`view-ferris`＝選你的型→**每天 10 位、首屏 5、按「再轉一批」翻出後 5、看完鎖定到明天、跨日重置**（localStorage `dopa_ferris_day`）。卡片：類型徽章／代號／自介／**「跟這型相處」提示**＋想認識鈕。**後端串接完成（2026-08-18）**：approved_profiles view 真名單（含暱稱審核狀態）、invites 表送出（RLS 擋重複＋封鎖雙向擋）、登入後自動選型。**每日換新完成（2026-08-18）**：LCG 日期種子 shuffle（fetch 50 筆→seededShuffle(arr, daySeed())→取前 10），同一天所有人看到的順序相同、跨日自動換新一批。**仍待**：招呼先審後顯（D2）。 |
| B3 | 🎢 雲霄飛車＝隨機盲配「上車就配」 | ✅ | 2 | **前端抽卡空殼已建（2026-07-29，先挑一區做深）**：`view-coaster`＝上車→拉霸式滾動 emoji→定格抽一位巴巴卡（類型徽章／匿名代號／自介真心話／能量 tag×3）＋想回應（彈提示）。**每日一抽＝回訪引擎**：抽完即鎖定「明天再上車」。**後端串接完成（2026-08-18）**：approved_profiles 真資料每日一抽、當日鎖定跨日解鎖（localStorage 記 user_id 跨 reload 復原同一張）、invites 送出。**後端防刷完成（2026-08-18）**：daily_coaster 表 DB 記錄每日一抽（initDaily DB 優先 fallback localStorage；todayStr 補零避免跨月日匹配錯誤）；登入用戶抽後 upsert daily_coaster，改快取或換裝置仍抽同一位。**仍待**：回應先審後顯（D2）。 |
| B4 | ☕ 旋轉咖啡杯＝真人老師講解市集 | ✅前端 | 獨立軌 | **前端市集空殼已建（2026-07-29）**：`view-teacup`＝付費階梯條（AI 99 入口→真人老師升級）＋4 張示範老師卡（頭像／化名／專長標籤／風格／評價星／價格／錄音交付／預約鈕）＋老師招募入口。老師專長＝服務類別描述、不碰命理斷言。大廳熱區改「開放」＝**大廳四區全開**。**✅ 預約整條流程已建（2026-08-03）**：老師個人頁、預約表單、結帳（共用 A3）、我的訂單（狀態鏈）、交付區、評價。**老師招募申請表完成（2026-08-18）**：「申請成為老師」按鈕 → 彈出 Modal 表單（姓名／聯絡方式／專長標籤／自介）→ 寫入 teacher_applications 表；重複申請回 23505 錯誤碼顯示「已送出」；成功後 3 秒自動關閉。**⚠️ 仍缺**：**老師後台**（接單／上傳錄音／看拆帳收益）＝完全沒有。**待後端**：招募審核老師＋拆帳金流＋真預約＋真錄音交付＋評價送審。 |
| B5 | 💌 私訊層：配對後聯絡 | 🟡前端＋後端部分 | 1（跟 B2 綁） | **前端空殼已建（2026-08-01）**：`view-inbox`＝nav 新增「💌 信箱」分頁（未讀紅點）＋三分頁＋對話頁。**預設分頁＝「聊天中」的對話列表**（2026-08-01 使用者指正：信箱打開第一眼要看到「誰在跟我聊」，不是邀請；原本預設停在「邀請」害列表看起來不存在）。列表每列＝代號／類型／最後一句（自己說的加「你：」）／輪到誰／時間，配幾個人就幾列。有人敲你時列表上方出現黃色橫幅「有 N 個人想認識你 →」點了跳「邀請」分頁；沒邀請就不出現。摩天輪「想認識」與雲霄「想回應」→ 呼叫 `window.DopaDM.invite()` 進「已送出」（同一人不重複敲）。對話頁＝泡泡串／輪流制（送出後鎖輸入框、換成「等對方回你」提示）／200 字上限／⋯選單（結束・封鎖・檢舉，皆單方生效不通知對方）／示範用假回覆 2.2 秒後回一則。暱稱可改（存 localStorage `dopa_dm_v1`）。**本機驗證全過**：個資偵測真值表 12/12（7 命中含 ig／手機／加賴／email／line／@handle／留電話，5 不誤判含「依賴」「信賴」「無賴」「我的手機」）、警語按確定照送、200 字擋、輪流制擋、結束線、跨 reload 保存、桌機＋手機皆無水平溢出、console 無錯。<br>**機制已拍板（2026-08-01，規劃書 §11）**：①雙向同意才開線（A 按想認識→B 的 LINE 收通知→B 按回應才建線；不理 7 天過期、不通知 A 被拒）②即時送達＋**輪流制**（一次一則、對方回了才能再送、≤200 字）③預設代號＋類型徽章、**暱稱可自編**（暱稱屬公開資訊→走公開區審核）④任一方可結束／封鎖／檢舉，結束不通知對方 ⑤**私訊不審**（見 D2）⑥外流聯絡方式＝**本地偵測跳警示、按確定照送**，不阻擋、不提供交換功能，想深聊導去咖啡杯找真人老師。**後端串接完成（2026-08-18）**：invites／threads／messages 表全通；accept_invite RPC 原子建線；Realtime WebSocket 即時送達；封鎖 RLS 已補（被封鎖者雙向無法重發邀請）；檢舉自動封鎖並存最後 10 則對話快照至 reports 表。**封鎖名單管理完成（2026-08-18）**：會員中心「封鎖名單」區（#blockList）→ 列出所有我封鎖的人（從 approved_profiles 拿顯示名稱）＋每人一顆「解除封鎖」按鈕（DELETE from blocks）。**仍待**：LINE OA 通知（D1 Phase 1）。 |

**社交三鐵則**：①依類型交流、講互動 dynamics 不講天生一對　②<100 字自介、嚴禁洩漏性別　③**匿名 × 輪流制**（2026-08-01 修訂：原「異步・無即時聊天」→ 私訊改**即時送達**，慢節奏改靠「一次一則、對方回了才能再送」守）

**前端進度（2026-07-28）**：新增「大廳」分頁＝四區導覽卡（🎠 樹洞開放／🎡🎢☕ 即將開放佔位）＋ B1 樹洞頁（投稿表單＋紙條牆）已建為純前端空殼、實測可運作。三鐵則都體現在 UI（不問性別引導欄／依五型身份／匿名異步紙條牆）。B2/B3/B4 待後端。原始碼在 `_template.html`（跑 inject.py 生 index.html）。

**大廳介面 2.0（2026-07-29 ✅收尾完成・本機驗證通過、待重發 Artifact）**：大廳從「四區導覽卡」重做成**精緻島嶼地圖＋整圖切換 hover**。機制＝5 張使用者 PS 合成的地圖疊層（`大廳/大廳_v2.0.png` 空基底＋`v2.1` 木馬小么／`v2.2` 摩天輪月月生生／`v2.3` 雲霄阿衝／`v2.4` 咖啡杯阿亨），四透明熱區蓋在四平台上、CSS `:has()` hover 切換整圖，樹洞熱區 `data-go=tree` 可點進投稿頁。原始碼＝`_template.html` 的 `view-lounge`（`.map` 內 5 個 `.layer` ＋ 4 個 `.hot`）。**收尾 4 項全完成（2026-07-29）**：①✅ 5 圖瘦身 WebP（1280×720／各 ~120KB／5 張共 620KB）已 inject base64（template `src="__LOUNGE0-4__"` → inject `LNG0-4` 進 IMG map、殘留佔位驗 0；index.html 2.74→3.56MB）——**單檔 Artifact 部署地雷解除** ②✅ 名牌四區座標對齊設施中心（`.hot-carousel/ferris/coaster/teacup` 改 left-based、中心約 29/72/29/72%，下排 y 收回島內）③✅ 即將開放三區加 `data-soon` → 點了彈 toast「敬請期待」，樹洞區維持 `data-go=tree` 可點進投稿 ④✅ 舊檔（`大廳.png`＋8 張 `大廳_00xx_*`＋`_contact.png`）移進 `大廳/_舊版棄置/`（未硬刪、可復原）。**本機 http server（127.0.0.1:8765）render 驗證**：hover 整圖切換正常（摩天輪→生生月月現身）、toast 正常（soonCount 3、click→show）、名牌到位。✅ **已上站（2026-07-29）**：Claude 用 Artifact 工具把本機新版重發到原 URL（`0a24bc5f…`），連結不變（發佈流程見網站記憶「重發 Artifact SOP」）。inject.py 的 `datauri` 支援 by-ext mime。

> ✅ **島嶼地圖大廳凍結已解、地圖已復活（2026-07-29 晚，使用者實測抓包＋修復）**：原本點「大廳」整頁凍死（連帶害從大廳進的摩天輪看起來空的＝使用者原報「摩天輪空的」真因）。**根因＝對大圖套「圓角裁切」**（`.map{border-radius:28px; overflow:hidden}` 把大 WebP 裁成圓角，沙盒 compositing 會炸；同尺寸首頁 hero 無裁切就正常）。**解＝拿掉 `border-radius`＋`overflow:hidden`，底圖像 hero 一樣單純 `<img width:100%>` 顯示** → 真 Artifact 實測不凍、島嶼地圖正常、4 熱區可點導覽。中間一度退成無圖卡片是止血，確認裁切主因後已復活。**✅ 設施動畫版完成（2026-07-30 上站實測）**：使用者 PS 四張設施＋泥巴寶合成圖（綠底）→ Higgsfield Kling 3.0 image-to-video 各生 3s → ffmpeg 綠幕去背 → **APNG**（200px/fps10/256色調色盤，四支共 ~1MB）→ 疊在大廳底圖的四座設施上，**永久循環播放**（木馬馬匹起伏＋小么、摩天輪轉＋月月生生、雲霄阿衝衝下坡出畫、咖啡杯阿亨搖擺）。惰性掛載（進大廳才掛 src、離開卸、錯開 450ms）。⚠️ 重要教訓：**動畫 WebP 在 Artifact 沙盒必崩 renderer（試三版全崩），APNG 走 PNG 解碼路徑才活**——完整管線與參數見 [[artifact-sandbox-gotchas]]。（先前的 hover-reveal 靜態 mascot 版已被此版取代）
> **最終版 = 7/30 00:47 build，index.html 4.62MB**（大廳底圖從 WebP 換回 `大廳_v3_空.png` 256 色 PNG——ffmpeg 轉的 WebP 版也會觸發沙盒解碼崩潰，PNG 路徑實證安全；因此體積比先前記的 4.17MB 大）。**✅ 2026-08-01 核對：線上 artifact payload 與本機 `index.html` 逐位元一致（4,619,468 bytes／md5 `dc68828a43a65c2b5185bb934c534145`），確認已上站、無待重發。**
> **同批修好（2026-07-29 晚）**：① A5 圖卡下載——沙盒無 `allow-downloads`、`a.download` 被擋 → 改「產生圖卡→內嵌顯示在頁面，長按/右鍵存」，SVG/PNG 全走 data: 繞過 blob CSP（本機驗證產出未污染 PNG）② 深度解析「99 解鎖」加 `verified` 旗標——沒真的跑過驗證就按解鎖 → 擋下並導去「巴巴驗證」（不再解鎖示範資料）。

---

**全站介面稽核 ＋ 修補（2026-08-03）**：掃過 14 個 view，補掉的與仍缺的分開記。

**修掉的**：
- **兩個「找真人老師」CTA 都指錯頁**——深度解析頁那顆 `data-go="verify"`（跑去驗證機）、摩天輪牆底那顆 `data-go="unlock"`（跑去 AI 99 頁）。加上 `teacup` 當時在登入閘門名單裡，等於**高價升級路徑被錯連結＋登入牆雙重擋死**。兩顆都改指 `teacup`，並把 **teacup 移出 `GATED`**（買東西不需要登入；社交區要登入是因為要記住你是誰、誰在跟你聊，購買沒這個必要）。咖啡杯返回鈕改 `data-back`（可能從大廳或深度解析進來，不能寫死回大廳）。
- **出生資料原本只活在記憶體**（表單欄位＋`window.__lastChart`），重新整理就沒了 → 預約老師會卡住、會員中心「出生資料」永遠顯示還沒填。改成驗證成功時存 `dopa_birth_v1`，reload 後回填表單、預約與會員中心都讀得到。**待後端**：登入後這份要存進帳號（見交接規格 `birth_profiles`），不是只留在裝置。

**仍缺的介面（本輪沒做，依重要性）**：
1. ~~自我介紹編輯~~ **✅ 已補（2026-08-03，見 C2）**——原本摩天輪／雲霄的卡片每位都有自介，但你自己的自介沒有任何輸入介面（onboard 只問暱稱），等於進得了配對牆、別人看到的你是空白。
2. ~~**檢舉表單**~~ **✅ 已補（2026-08-18）**：檢舉自動擷取最後 10 則對話快照存 reports 表、同時封鎖對方（RLS 雙向擋）。政策承諾已對齊。~~**仍缺**：封鎖名單管理（看誰被我封了／解除）。~~ **✅ 封鎖名單完成（2026-08-18）**：會員中心新增封鎖名單區＋解除封鎖。
3. ~~**老師招募申請表**~~（見 B4）**✅ 完成（2026-08-18）**。**仍缺**：老師後台（D4）。
4. ~~**樹洞「我的紙條」狀態頁**~~（見 B1）**✅ 完成（2026-08-18）**。
5. ~~**公開內容審核後台**~~（紙條／自介／暱稱）**✅ 完成（2026-08-18，見 D2）**：DB trigger 自動初篩＋管理員後台。
6. ~~**通知中心**~~ **已消化（B5 完成後）**：nav 紅點＝信箱未讀數（`#navUnread`），點了進 `view-inbox`；B5 Supabase 後端串完後此紅點已有實際資料驅動，不需另建通知頁。LINE 推播通知（D1）待後端 Webhook。
7. **驗證機出生地**只有 16 個地區下拉、歐美澳走固定 offset 不處理 DST——**這是會算錯一整型的已知缺陷**，精算版必須補真城市＋時區資料庫。

**Bug 修補（2026-08-18）**：
- **LINE 顯示「Google 登入」**：LINE 走 Edge Function magic link 時 `public.users.provider` 被 trigger 誤寫 'google'；前端補 fallback 偵測 `session.user.app_metadata.provider` / `providers` 陣列 / `line_` email 前綴。
- **自介「已過審 ✓」badge 重複出現**：`paintAccount()` 在登入後連發多次，async callback 每次讀 `introEl.textContent` 時已包含前一次 badge 文字而疊加；改成函式一開始就捕捉 `_baseIntro` / `_baseNick`，async callback 閉包引用固定基礎值。
- **`users?select=is_admin...` 400 × 5**：`is_admin` 欄位尚未建立（需跑 ALTER TABLE SQL）＋ `paintAccount()` 連發 5 次。前端加 80ms 防抖（`clearTimeout(paintAccount._t)`）確保只打一次 DB；`if(r.error||!r.data) return` 靜默處理欄位不存在的 400。

**⚠️ 本機領先線上**：8/2–8/3 這批（滑動卡牌、會員系統、大廳登入閘門、首頁大廳區、導覽列、凍選單、去 emoji，加上今天這批結帳與老師預約）**都還沒發佈**，線上 artifact 停在政策頁那一版。要發＝跑 `inject.py` → 剝掉開頭四行 → 用原 URL 發佈。

---

## C. 配對內容（送審知識庫後才發）

| # | 任務 | 狀態 | 備註 |
|---|---|---|---|
| C1 | 五型 × 五型 互動小卡 ~25 則 | ✅ | **完成（2026-08-18）**：25 組（5×5）互動提示依觀看者的型動態顯示；每則三段：互補／卡點／提醒。內容從 `hd-content.js` TYPE 物件推導（策略・非自己情緒・能量場特性），只定原則不編偽對照。`TIP5[viewerType][cardType]` 結構，觀看者型從 `U.type` 取得；CSS 補 `.pt-c/.pt-x/.pt-t` 三色標籤。顯示於摩天輪配對牌卡的「跟這型相處」區塊。 |
| C2 | 自介引導模板題目 | ✅前端 | **已建（2026-08-03）**：`view-intro`＝①**4 個引導題目**（最近有感的一件事／我卡住的時候／我希望被理解的／我能量最順的時候）點了幫你起頭，只問能量與感受、不誘導講身分外貌 ②自介 **100 字上限** ③**能量標籤走 18 個固定清單、選 1–3 個**（不開放自由輸入＝從源頭杜絕夾帶個資）④**即時預覽**用真的 `.pair-card` 結構，看到的就是摩天輪／飛車上別人看到的你。<br>**性別／聯絡方式偵測＝命中直接擋下不讓送**（跟私訊的「警示但照送」刻意不同調：私訊是隱私優先平台不讀，自介是公開區走先審後顯）。**真值表 12/12 驗過**：擋下男生／女生／老公／當媽、加賴／我的ig／手機號／@handle；不誤擋依賴、信賴、無賴、online。刻意不收「他／她」「爸爸／媽媽」——中文常當通稱、或講的是別人不是自己，收了只會製造誤擋。<br>**接進流程**：登入→暱稱→**自介**→才回原本要去的那一區（沒自介就進配對牆＝別人看到一張空卡）；會員中心「帳號設定」加一列「大廳自介」可隨時改。**待後端**：自介要進帳號、走 AI 初篩→先審後顯，並餵給摩天輪／雲霄的真實名單（現在牆上仍是假資料）。 |

---

## D. 支撐系統

| # | 任務 | 狀態 | 備註 |
|---|---|---|---|
| D1 | LINE OA 申請設定 | ✅ | **已申請並設定完成（2026-08-18）**：LINE OA 建立完成；啟用 Messaging API；Linked OA 已在 LINE Login 頻道連結完成；免費方案每月 200 則確認足用。**Phase 1 通知完成（2026-08-18）**：Edge Function `handle-invite-event` 部署；PostgreSQL trigger（`on_invite_insert` / `on_invite_update`）用 pg_net 呼叫 Edge Function；新邀請通知收件人、邀請被接受通知發送者；只有 LINE 登入用戶（`provider='line'`）收到推播，Google 用戶靜默跳過。`LINE_CHANNEL_ACCESS_TOKEN` 已設為 Supabase secret。LINE Developer Console Webhook URL 為選配（接收用戶主動傳訊用），推播不需要。 |
| D2 | 三層審核 ＋ 18+ 互動層 | ✅ | **範圍縮小到「公開區」（2026-08-01）**：私訊層不審、平台不主動閱讀（隱私安心感）。**自動初篩完成（2026-08-18）**：PostgreSQL BEFORE INSERT/UPDATE trigger 跑 regex → 乾淨內容自動 approved、命中性別詞／聯絡方式留 pending。涵蓋 lounge_posts / profiles / users.nick 三張表。**人工審核後台完成（2026-08-18）**：`view-admin`（管理員後台）三個 Tab（紙條／自介／暱稱），每筆可一鍵核准或駁回＋可附駁回原因；只有 `users.is_admin=true` 的帳號進得去；RLS 政策補齊（admin_select / admin_update）。**用戶端審核結果顯示完成（2026-08-18）**：會員中心暱稱欄顯示「審核中」badge 或「未通過，請重新設定」；大廳自介欄顯示「審核中」badge、「未通過・[駁回原因]」或「已過審 ✓」——透過進帳號頁時 async 查 DB（不依賴 localStorage 快取）。**待補（不擋上線）**：①服務條款補寫「不主動審閱私訊、依檢舉處理」（D5 待補欄已記）②目前初篩是 regex；日後量大可在 trigger 內改呼叫 LLM API 做語意判斷 |
| D6 | 會員系統：介面＋資料庫規格 | ✅ | **前端已建（2026-08-01）**：`view-auth`（Google／LINE 兩顆＋18+ 與條款同意勾選，未勾擋下）→ `view-onboard`（暱稱＋即時預覽「別人會看到什麼」，可跳過）→ `view-account`（身份卡／四張捷徑／帳號設定／刪除帳號）；nav 右側 `#navUser` 未登入顯示「登入」、登入後顯示 provider 圖示＋名稱。**只做社群登入、不做 email／密碼**（不存密碼＝不扛外洩責任）。**後端已完成（2026-08-18）**：Google OAuth＋LINE Login 端對端通過；DB trigger 自動同步 public.users；出生資料、計算結果（charts）、暱稱審核狀態（nick_status）、同意條款版本（terms_version）、軟刪除（deleted_at）全數建立；私訊、審核、封鎖、檢舉等 API 均已串接。**待後端（等金流）**：orders 後端串接、entitlements 解鎖權限、帳號刪除後交易紀錄去識別化保留 5 年。 |
| D3 | 後端選型拍板 | ✅ | **已選定 Supabase（2026-08-18）**：專案 tylikhvklfxctqnjpbqf；Google OAuth 走 Supabase 內建 provider、LINE Login 走 Edge Function（line-callback）＋魔術連結，兩者均端對端測試通過；DB Schema 14 張表已部署、handle_new_auth_user trigger 已建（含 custom:line 處理）；Supabase CLI 已安裝並登入。 |
| D5 | 政策三頁（footer 連結） | ✅上站 | **隱私權政策**（個資法要求、缺了可罰 2–50 萬，且 Google／Meta／TikTok 投廣告會審這頁；必備段落＝總則／蒐集範圍／利用目的／保護措施／第三方分享／保存期限／用戶權利＋政策變更＋聯絡方式＋修訂日期）／**使用者條款**（「不主動審閱私訊、依檢舉處理」＋18+ 寫這裡）／**社群守則**（公開區審核標準、私訊違規停權級距、檢舉流程）。**產品介面內不自證清白**（2026-08-01：信箱頁那段隱私自白已刪，只留「⋯ 可以封鎖或檢舉」一行功能指引）。**✅ 三頁已寫完並上站（2026-08-01）**：文案定稿在 `多巴樂園_政策文案_v1.md`，網頁在 `view-policy`（footer 三顆連結 → 子分頁切換）。已填：營運者＝**創巢數位資訊企業社**／客服＝**chiehhh1998@gmail.com**／管轄＝**台灣桃園地方法院**（登記地桃園）／退款＝**概不退款**（結帳頁必須有「同意即時交付、不適用七日鑑賞期」勾選，見 A3）／保存期限＝帳號刪除後 30 天・私訊結束後 30 天・交易 5 年・檢舉 2 年／個資法處理期限＝查閱 15 日、更正刪除 30 日（各得延長同天數）。**待補（不擋上線）**：①~~第三方服務商具名（等 D3）~~ **✅ 已填（2026-08-18）**：Supabase／LINE OA／Google Login／LINE Login（金流商仍待定，政策文案保留佔位）②老師抽成 %（現寫「實際比例將於招募說明與上架時公告」）。 |
| D4 | 老師後台（接單・上傳錄音・收益） | 🔧部分完成 | **已完成（2026-08-20）**：`teachers` 表建立；`orders` 擴充 `teacher_id / plan_name / plan_price / teacher_status / recording_url / teacher_accepted_at / delivered_at`；老師市集從 Supabase 載入真實資料（有資料替換 mock）；會員中心顯示老師後台入口（偵測 `teachers` 表是否有自己的 row）；`view-teacher-dash` 老師後台三 Tab（訂單接單/拒接、上傳錄音至 `recordings` Storage bucket、收益試算）；老師可上下架自己；RLS 政策設定完成（`teachers` 表自讀自改、`orders` 老師端讀寫、Storage 上傳下載）。**待完成（blocked by A3 金流）**：實際訂單流（客戶付款後 `orders.status=paid` 才會進老師待接單）；老師錄音後客戶端在 `view-orders` 聽取（需後端整合訂單）。**待決定**：老師抽成 % 數（目前 DB 預設 20%，政策文案留佔位）；老師市集正式招募與審核機制（命理正確性把關）。 |

---

## E. 素材

| # | 任務 | 狀態 | 備註 |
|---|---|---|---|
| E1 | Hero 主視覺補第五寶（生生） | ✅ | 查證 7/28：**單隻五寶 PNG 已同步**（A/G/MG/P/R = IP/ 最新）。LOGO/ICON 站上是資料夾檔的網頁優化版（壓縮／去背／縮小）、視覺一致，**無需動**。真缺口＝hero 遊樂園插畫（composition／hero_cut／站上 HERO）仍是**舊四隻（紅黃藍紫），缺橙色生生（顯生）** → 要重畫一張五隻的 hero（生圖任務）。**7/28 補**：本環境 Adobe 工具無生成／合成能力（image_fill／text-to-image 皆不支援，僅 generative_expand 外擴），故走 **Nano banana pro 出圖**（拿 composition 當底＋@生生 角色庫補第五隻，提示詞已備）→ 白底圖回來後 **Adobe image_remove_background 去背**（此功能支援）→ 換 HERO → inject 上站。**✅ 完成 7/28**：Nano 生五寶 hero → Adobe 去背（四角 alpha=0、五隻＋設施完整）→ inject.py 的 HERO 改用 `頁首/hero_五寶_去背.png` → 重跑，首頁已顯示五寶、透明無縫融入。**已瘦身 7/28**：hero 降 1280px＋轉 WebP q90＝**237KB**（砍 87%），index.html 反而降到 **2.74MB**（比動工前 3.1MB、比舊四隻版都小）。`inject.py` datauri 已支援 by-ext mime；上站版＝`頁首/hero_1280.webp` |

---

## 待決事項（open items，做之前要拍板的）

- [x] ~~後端選型（Supabase／Firebase／其他）與成本估算~~ ✅ 已選定 Supabase（2026-08-18）→ D3
- [ ] LINE OA 當前台灣推播級距與費用 → D1
- [ ] 老師市集抽成 **%數**（已定要抽成，%待定）→ D4
- [ ] 付款 → 解鎖分析後，是否自動獲得大廳入場資格（整合點）→ A4／B
- [ ] 上線名額／檔期（是否呼應「限量」打法）
- [ ] 先審後顯誰來審、審核 SLA（**僅公開區**）→ D2
- [x] 私訊每日則數上限：**不做**（2026-08-18 拍板）——輪流制已天然限速，後續若有濫用問題再加；邀請 7 天過期已實作（`expires_at` 欄位＋前端查詢過濾）→ B5

---

## 已定案（不再討論）

- AI 分析定價 = **99 元 flat**（單一價，取消舊 98／168 分級；深／完整交給真人老師）
- 階梯：**AI 文字 99（入口・即時・無限量）→ 真人老師語音（對症・老師定價・平台抽成）**
- 老師交付 = **錄音**（非即時，跟樹洞慢節奏一致）
- 社交定調 = **樹洞＋學表達，不是約會**；隱藏性別把焦點拉回能量與想法
- 收款先於出解讀（數位商品不做一手交一手）
- **大廳全區＋信箱＝強制登入**（2026-08-01）：`GATED={lounge,tree,ferris,coaster,teacup,inbox}`，未登入點了會被導到登入頁並顯示「大廳需要登入 🎡」之類的對應原因，登入＋補完資料後**自動回到原本要去的那一區**（不是丟到會員中心）。免費驗證／巴巴們／深度解析／政策頁**不擋**。首頁不改版，只在「深度解析預告」後面加一段大廳預告（四區小卡＋CTA＋一句說明為什麼要登入），驗證結果卡也加了「🎡 進大廳認識別種巴巴」。
- **配對後聯絡 = 站內私訊**：雙向同意才開線／即時送達＋輪流制／**私訊不審**／外流聯絡只本地跳警示不阻擋（A 案・不提供交換聯絡功能）／暱稱可自編但預設代號＋類型徽章（2026-08-01，細節見規劃書 §11）
