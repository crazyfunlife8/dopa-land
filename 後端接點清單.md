# 後端接點清單（自動產生）

由 `frontend/js/app.js` 掃出，共 **12** 處 `TODO(後端)`。行號對應該檔。

## TODO(後端) 標記

- **app.js:84** — 這裡所有 TODO(後端) 標記的地方，都是前端等後端給 API 的接點。
- **app.js:134** — TODO(後端)：這裡改成導去後端的 OAuth 授權網址， 後端拿到 provider 的 profile 後建立／登入使用者，再導回本頁並帶回 session。
- **app.js:156** — TODO(後端)：暱稱要送審（公開內容先審後顯），通過才顯示給其他人
- **app.js:185** — TODO(後端)：同時要讓後端 session 失效
- **app.js:191** — TODO(後端)：呼叫刪除帳號 API；隱私權政策承諾 30 天內清除
- **app.js:200** — TODO(後端)：LINE OA 綁定流程
- **app.js:309** — TODO(後端)：這只是本地初篩。公開內容仍要走 AI 初篩 → 命中攔／送人工，前端擋不住繞過的人。
- **app.js:671** — TODO(後端)：按讚要打 API 累計、同一帳號同一張只能一次。 現在只存在這台裝置的 localStorage，清快取就能重按（前端擋不住刷讚）。
- **app.js:1071** — TODO(後端)：coPay 要改成建立訂單 → 取得金流服務商的付款網址／取號結果 → 導轉； 付款結果必須以服務商的「伺服器端回拋（背景通知）」為準，不能信前端導回的參數。
- **app.js:1195** — TODO(後端)：真版不會有這顆按鈕——入帳由金流服務商回拋通知，前端只負責顯示
- **app.js:1288** — TODO(後端)：評價要送審＋只有完成交付的訂單能留，並回寫老師的平均分
- **app.js:1597** — TODO(後端)：登入後這份要存進帳號（見交接規格 birth_profiles），不是只留在這台裝置。

## 前端目前用到的 localStorage key

每一個都是「現在假裝有後端」的暫存，正式版要換成 API＋資料表：

- `dopa_birth_v1` — 出生資料（日期／時間／時區）→ 對應規格書 birth_profiles
- `dopa_coaster_draw` — 雲霄每日一抽狀態
- `dopa_dm_v1` — 私訊：邀請／對話／暱稱 → 對應規格書 messages
- `dopa_ferris_day` — 摩天輪每日配額狀態
- `dopa_orders_v1` — 訂單（AI 99 與老師單共用）→ 對應規格書 orders
- `dopa_profile_v1` — 大廳自我介紹＋能量標籤（公開內容，要先審後顯）
- `dopa_tree_likes_v1` — 樹洞按讚（我按過哪幾張）→ 需後端計數＋防刷
- `dopa_user_v1` — 登入者（示範版假帳號）→ 對應規格書 users

## 圖檔對照（KEY → 檔名）

`app.js` 開頭的 `IMG` 物件是 KEY → 路徑的對照表；HTML 用 `data-img="KEY"` 取圖。

| KEY | 檔案 | 型別 | 大小 |
|---|---|---|---|
| `A` | `assets/blob-manifestor.png` | image/png | 264 KB |
| `ANC` | `assets/ride-carousel.apng` | image/apng | 400 KB |
| `ANF` | `assets/ride-ferris.apng` | image/apng | 300 KB |
| `ANR` | `assets/ride-coaster.apng` | image/apng | 116 KB |
| `ANT` | `assets/ride-teacup.apng` | image/apng | 226 KB |
| `G` | `assets/blob-generator.png` | image/png | 251 KB |
| `HERO` | `assets/hero.webp` | image/webp | 237 KB |
| `IC1` | `assets/step-1-birth.png` | image/png | 43 KB |
| `IC2` | `assets/step-2-machine.png` | image/png | 44 KB |
| `IC3` | `assets/step-3-blob.png` | image/png | 51 KB |
| `LNG0` | `assets/lounge-map.png` | image/png | 312 KB |
| `LOGO` | `assets/logo.png` | image/png | 164 KB |
| `MG` | `assets/blob-mfg-generator.png` | image/png | 253 KB |
| `P` | `assets/blob-projector.png` | image/png | 265 KB |
| `R` | `assets/blob-reflector.png` | image/png | 263 KB |
